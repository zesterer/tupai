include_rules

INCLUDES=$(INCLUDES) -I../../include

# Compiling
ifeq (@(TARGET_ARCH),x86_64)
	: foreach *.s |> !ASM |>
	: foreach *.c |> !CC  |>
else
ifeq (@(TARGET_ARCH),i386)
	: foreach *.s |> !ASM |>
	: foreach *.c |> !CC  |>
endif
endif
