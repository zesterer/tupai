# Commands

RUSTC_EXEC=rustc

# Flags

ifeq (@(TARGET_ARCH),x86_64)
	RUST_TARGET=x86_64-unknown-linux-gnu
endif

RUST_FLAGS=--crate-type=staticlib --target=$(RUST_TARGET)

# Compilation commands

!RUSTC = |> $(RUSTC_EXEC) $(RUST_FLAGS) -o %o %f |> %B.o
